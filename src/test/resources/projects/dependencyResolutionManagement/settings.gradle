pluginManagement {
	plugins {
		id 'dev.architectury.loom-repositories'
	}
}

plugins {
	id 'dev.architectury.loom-repositories'
}

dependencyResolutionManagement {
	repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
}

include 'basic'
include 'projmap'